name: Sync UNC Futbol League data

on:
  workflow_dispatch:
  schedule:
    - cron: '15 5,11,17,23 * * *'

permissions:
  contents: write

concurrency:
  group: virtual-arena-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
      - name: Sync competition 1 season 1
        run: node scripts/sync-virtual-arena.mjs
      - name: Publish changed league data
        run: |
          if git diff --quiet -- pickems-app/season-data.js pickems-app/season-data.json; then
            echo "UNC Futbol League data is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pickems-app/season-data.js pickems-app/season-data.json
          git commit -m "Sync Virtual Arena league data"
          git push
